{
  "name": "extractor",
  "version": "1.0.0",
  "description": "Extract the .in and mm from pdf input",
  "main": "app.js",
  "scripts": {
    "start": "electron app.js",
    "build": "electron-builder build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "V.V.S.K",
  "license": "ISC",
  "devDependencies": {
    "electron": "^21.3.1",
    "electron-builder": "^23.6.0"
  },
  "dependencies": {
    "pdfreader": "^2.0.0"
  }
}
